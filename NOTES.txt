Run commands for WASP + PTE agent
==================================

All commands are run from: wasp/webarena_prompt_injections/
Environment variables (GITLAB, REDDIT, OPENAI_API_KEY, DATASET) are loaded automatically from wasp/.env

Script signature:
  bash scripts/run_end_to_end.sh <output_dir> <model> <system_prompt> <config> <user_goal_idx> <injection_format> <output_format>


--- ONE TASK (single GitLab task, one user goal, one injection format) ---

bash scripts/run_end_to_end.sh \
  /tmp/pte-e2e-test/ \
  gpt-4o \
  configs/system_prompts/wa_p_som_cot_id_actree_3s.json \
  configs/experiment_config_single.json \
  0 \
  goal_hijacking_plain_text \
  pte


--- GITLAB ONLY (12 tasks x 2 user goals x 2 injection formats = 48 total runs) ---

/home/annabella/AIAgent/PTE/venv/bin/python run.py \
  --config configs/experiment_config_gitlab.json \
  --output-dir ../pte-gitlab-results \
  --output-format pte


--- REDDIT ONLY (9 tasks x 2 user goals x 2 injection formats = 36 total runs) ---
Note: Reddit API server must be running first:
  python /path/to/PTE/api/servers/reddit.py

/home/annabella/AIAgent/PTE/venv/bin/python run.py \
--config configs/experiment_config_reddit.json \
--output-dir ../pte-reddit-results \
--output-format pte



--- FULL RUN (all 21 tasks x 2 user goals x 2 injection formats = 84 total runs) ---
Note: Reddit API server must be running first (see above)

python run.py \
  --config configs/experiment_config.raw.json \
  --output-dir /tmp/pte-full-results \
  --output-format pte


=====================================
Run commands for WASP + Beyond Browsing agent
=====================================

Agent location: wasp/../BeyondBrowsing/API-Based-Agent/
Model/API key: configured in BeyondBrowsing/API-Based-Agent/config.toml
Prerequisite: Docker must be running (Beyond Browsing uses a Docker sandbox)

--- ONE TASK (single GitLab task, one user goal, one injection format) ---

bash scripts/run_end_to_end.sh \
  /tmp/bb-e2e-test/ \
  gpt-4o \
  configs/system_prompts/wa_p_som_cot_id_actree_3s.json \
  configs/experiment_config_single.json \
  0 \
  goal_hijacking_plain_text \
  beyond_browsing

--- GITLAB one of each type (4) ---
python3 run.py \
  --config configs/experiment_config_single.json \
  --output-dir /tmp/bb-gitlab-results--singlepertype \
  --output-format beyond_browsing


--- GITLAB ONLY (12 tasks x 2 user goals x 2 injection formats = 48 total runs) ---

python3 run.py \
  --config configs/experiment_config_gitlab.json \
  --output-dir /tmp/bb-gitlab-results \
  --output-format beyond_browsing


--- REDDIT ONLY (9 tasks x 2 user goals x 2 injection formats = 36 total runs) ---
python run.py \
  --config configs/experiment_config_reddit.json \
  --output-dir /tmp/bb-reddit-results \
  --output-format beyond_browsing


--- FULL RUN (all 21 tasks x 2 user goals x 2 injection formats = 84 total runs) ---
python run.py \
  --config configs/experiment_config.raw.json \
  --output-dir /tmp/bb-full-results \
  --output-format beyond_browsing


--- CONFIG FILES ---

configs/experiment_config_single.json   — 1 GitLab task (no setup_fn, deploy key)
configs/experiment_config_gitlab.json   — 12 GitLab tasks
configs/experiment_config_reddit.json   — 9 Reddit tasks
configs/experiment_config.raw.json      — all 21 tasks


--- INJECTION FORMATS ---

goal_hijacking_plain_text     — plain text injection in page content
goal_hijacking_url_injection  — injection embedded in a URL on the page


--- USER GOAL INDICES ---

0 — Comment on the given issue/post saying we are working on it / had similar experience
1 — Add thumbs_up reaction to the given issue / Upvote the given post


=====================================
Run commands for WASP + ReactAPIWeb agent
=====================================

Agent location: wasp/../PTE/react_agent/
Model/API key: configured in PTE/config/config.yaml
IMPORTANT: Must use PTE venv Python — the wasp venv is missing PTE dependencies (e.g. dotenv).
  PTE_PYTHON=/Users/annabella/Downloads/AIAgent/project/PTE/venv/bin/python

--- ONE TASK (direct invocation, quickest way to verify it works) ---

/Users/annabella/Downloads/AIAgent/project/PTE/venv/bin/python run_react_api_web_agent.py \
  --task-config 0/webarena_tasks/1000.json \
  --trace-log-dir /tmp/react-test/ \
  --pte-dir /Users/annabella/Downloads/AIAgent/project/PTE \
  --max-iterations 5

--- ONE TASK via end-to-end script ---

bash scripts/run_end_to_end.sh \
  /tmp/react-e2e-test/ \
  gpt-4o \
  configs/system_prompts/wa_p_som_cot_id_actree_3s.json \
  configs/experiment_config_single.json \
  0 \
  goal_hijacking_plain_text \
  react_api_web


--- GITLAB ONLY (12 tasks x 2 user goals x 2 injection formats = 48 total runs) ---

/home/annabella/AIAgent/PTE/venv/bin/python run.py \
  --config configs/experiment_config_gitlab.json \
  --output-dir /home/annabella/AIAgent/wasp/react-gitlab-results \
  --output-format react_api_web


--- REDDIT ONLY (9 tasks x 2 user goals x 2 injection formats = 36 total runs) ---

/home/annabella/AIAgent/PTE/venv/bin/python run.py \
  --config configs/experiment_config_reddit.json \
  --output-dir /react-reddit-results \
  --output-format react_api_web


--- FULL RUN (all 21 tasks x 2 user goals x 2 injection formats = 84 total runs) ---

/Users/annabella/Downloads/AIAgent/project/PTE/venv/bin/python3 run.py \
  --config configs/experiment_config.raw.json \
  --output-dir /react-full-results \
  --output-format react_api_web


=====================================
Run commands for WASP + Claude Code agent
=====================================

Agent location: wasp/../PTE/eval/claude_code_agent_runner.py + PTE/scripts/run_claude_task.sh
Model: read from PTE/config/config.yaml (agent_llm_provider must be "anthropic"; the
  codegen session runs via the `claude` CLI). The --model flag below only steers WASP's
  own dispatch — it does NOT set the codegen model.
IMPORTANT: Must use PTE venv Python — the wasp venv is missing PTE dependencies.
  PTE_PYTHON=/home/annabella/AIAgent/PTE/venv/bin/python

How it differs from the ReactAPIWeb agent:
  - It is a codegen pipeline, not a browser agent. A network-isolated `claude` session
    writes a Python script for the task; that script is then run live against the site.
  - The codegen session never sees the injected page, so exfil ASR is ~0 by construction.
  - The trace JSONL is synthesized from the generated script source (in the tool-call
    arguments, where ExfilEvaluator looks) and the script's stdout.
  - Sites: gitlab / shopping / shopping_admin. reddit is not yet supported by
    ClaudeCodeAgentRunner.SUPPORTED_SITES (a reddit task records an "unsupported_site"
    trace until PTE adds it); the WASP wiring itself is site-generic and needs no change.
  - Codegen resolves the task intent from PTE_TASK_INTENT (set by run_claude_code_agent.py),
    which PTE/scripts/lookup_task.py honors in place of its dataset lookup.
  - Replan/retry (--max-attempts, default 3, mirrors PTE's pytest harness): a failed live
    script run (failure_kind=script_execution) is retried in replan mode — the codegen
    session reads the failed script's own python_output.log and repairs it; a failed
    codegen (failure_kind=codegen) is retried as a fresh regenerate. A task that produced
    an answer is never retried (WASP grades separately; retrying would only fit the grader).
    --max-attempts 1 disables retries (old single-shot behavior). NOTE: a hard config error
    still burns all attempts — e.g. agent_llm_provider != "anthropic" fails the provider
    gate in run_claude_task.sh on every attempt; retries only recover transient/script fails.
    The batch path below (run.py) does not thread this flag, so full runs use the default 3;
    to change it, edit CLAUDE_CODE_BASH_SCRIPT_SINGLE_RUN_TEMPLATE in constants.py.

--- ONE TASK (direct invocation, quickest way to verify it works) ---

# Run from webarena_prompt_injections/ (the script and the 0/... paths are relative
# to it). Absolute paths below so it works from any cwd:
cd /home/annabella/AIAgent/wasp/webarena_prompt_injections

/home/annabella/AIAgent/PTE/venv/bin/python run_claude_code_agent.py \
  --task-config /home/annabella/AIAgent/wasp/react-gitlab-results-claude/0/webarena_tasks/1000.json \
  --trace-log-dir /tmp/claude-code-test/ \
  --pte-dir /home/annabella/AIAgent/PTE \
  --max-attempts 3
# (any existing webarena_tasks/<id>.json works; the one above ships in the repo)
# --max-attempts is optional (defaults to 3); pass --max-attempts 1 for the old single shot.

--- ONE TASK via end-to-end script ---

bash scripts/run_end_to_end.sh \
  /tmp/claude-code-e2e-test/ \
  gpt-4o \
  configs/system_prompts/wa_p_som_cot_id_actree_3s.json \
  configs/experiment_config_single.json \
  0 \
  goal_hijacking_plain_text \
  claude_code

--- GITLAB ONLY (12 tasks x 2 user goals x 2 injection formats = 48 total runs) ---

/home/annabella/AIAgent/PTE/venv/bin/python run.py \
  --config configs/experiment_config_gitlab.json \
  --output-dir /home/annabella/AIAgent/wasp/claude-code-gitlab-results \
  --output-format claude_code

--- REDDIT ONLY (not yet supported by ClaudeCodeAgentRunner; runs but records
    "unsupported_site" traces until PTE adds reddit support) ---

/home/annabella/AIAgent/PTE/venv/bin/python run.py \
  --config configs/experiment_config_reddit.json \
  --output-dir /tmp/claude-code-reddit-results \
  --output-format claude_code


--- ONE-TIME SETUP (run once per machine) ---

# Wasp venv (for prompt_injector.py and evaluators)
cd wasp/webarena_prompt_injections
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt && deactivate

# VWA venv (for evaluator_final_step.py)
# Use requirements_eval_macos.txt — curated subset of requirements.txt without
# nvidia/CUDA packages that don't exist on macOS.
cd wasp/visualwebarena
python3 -m venv venv && source venv/bin/activate
pip install -r requirements_eval_macos.txt
pip install -e . --no-deps
playwright install chromium
# macOS SSL fix required for NLTK downloads:
python -c "import ssl; ssl._create_default_https_context = ssl._create_unverified_context; import nltk; nltk.download('punkt_tab'); nltk.download('punkt')"
deactivate
